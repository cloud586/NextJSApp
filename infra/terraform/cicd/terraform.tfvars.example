# Optional manual federated_credentials are no longer needed for pipeline ARM
# connections — the ado stack creates WIF credentials from service connection outputs.
# display_name = "sutoremu-nextjsapp-cicd"

# federated_credentials = []
